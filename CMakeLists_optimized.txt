# OpenCog Unified - Optimized Build Sequence
# Generated by dependency_analyzer.py

cmake_minimum_required(VERSION 3.10)
project(opencog-unified VERSION 1.0.0)

# Set C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Add cmake directory to module path
set(CMAKE_MODULE_PATH 
    ${CMAKE_CURRENT_SOURCE_DIR}/cmake
    ${CMAKE_CURRENT_SOURCE_DIR}/cogutil/cmake
    ${CMAKE_MODULE_PATH}
)

# Optimized build sequence based on dependency analysis

# Level 0 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cogutil/CMakeLists.txt")
    add_subdirectory(cogutil)
endif()

# Level 1 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace/CMakeLists.txt")
    add_subdirectory(atomspace)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/moses/CMakeLists.txt")
    add_subdirectory(moses)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/language-learning/CMakeLists.txt")
    add_subdirectory(language-learning)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/relex/CMakeLists.txt")
    add_subdirectory(relex)
endif()

# Level 2 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cogserver/CMakeLists.txt")
    add_subdirectory(cogserver)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-rocks/CMakeLists.txt")
    add_subdirectory(atomspace-rocks)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-agents/CMakeLists.txt")
    add_subdirectory(atomspace-agents)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-dht/CMakeLists.txt")
    add_subdirectory(atomspace-dht)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-ipfs/CMakeLists.txt")
    add_subdirectory(atomspace-ipfs)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-websockets/CMakeLists.txt")
    add_subdirectory(atomspace-websockets)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-bridge/CMakeLists.txt")
    add_subdirectory(atomspace-bridge)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-metta/CMakeLists.txt")
    add_subdirectory(atomspace-metta)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-rpc/CMakeLists.txt")
    add_subdirectory(atomspace-rpc)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/unify/CMakeLists.txt")
    add_subdirectory(unify)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/pattern-index/CMakeLists.txt")
    add_subdirectory(pattern-index)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/spacetime/CMakeLists.txt")
    add_subdirectory(spacetime)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/dimensional-embedding/CMakeLists.txt")
    add_subdirectory(dimensional-embedding)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/generate/CMakeLists.txt")
    add_subdirectory(generate)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/lg-atomese/CMakeLists.txt")
    add_subdirectory(lg-atomese)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cheminformatics/CMakeLists.txt")
    add_subdirectory(cheminformatics)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/agi-bio/CMakeLists.txt")
    add_subdirectory(agi-bio)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/vision/CMakeLists.txt")
    add_subdirectory(vision)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/sensory/CMakeLists.txt")
    add_subdirectory(sensory)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/visualization/CMakeLists.txt")
    add_subdirectory(visualization)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/TinyCog/CMakeLists.txt")
    add_subdirectory(TinyCog)
endif()

# Level 3 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-restful/CMakeLists.txt")
    add_subdirectory(atomspace-restful)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/atomspace-cog/CMakeLists.txt")
    add_subdirectory(atomspace-cog)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/attention/CMakeLists.txt")
    add_subdirectory(attention)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/ure/CMakeLists.txt")
    add_subdirectory(ure)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/learn/CMakeLists.txt")
    add_subdirectory(learn)
endif()

# Level 4 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/pln/CMakeLists.txt")
    add_subdirectory(pln)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/miner/CMakeLists.txt")
    add_subdirectory(miner)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/benchmark/CMakeLists.txt")
    add_subdirectory(benchmark)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/asmoses/CMakeLists.txt")
    add_subdirectory(asmoses)
endif()

# Level 5 - Parallel builds possible
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/opencog/CMakeLists.txt")
    add_subdirectory(opencog)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/python-attic/CMakeLists.txt")
    add_subdirectory(python-attic)
endif()

# Explicit dependency relationships
if(TARGET atomspace AND TARGET cogutil)
    add_dependencies(atomspace cogutil)
endif()
if(TARGET cogserver AND TARGET cogutil)
    add_dependencies(cogserver cogutil)
endif()
if(TARGET cogserver AND TARGET atomspace)
    add_dependencies(cogserver atomspace)
endif()
if(TARGET atomspace-rocks AND TARGET cogutil)
    add_dependencies(atomspace-rocks cogutil)
endif()
if(TARGET atomspace-rocks AND TARGET atomspace)
    add_dependencies(atomspace-rocks atomspace)
endif()
if(TARGET atomspace-restful AND TARGET cogserver)
    add_dependencies(atomspace-restful cogserver)
endif()
if(TARGET atomspace-restful AND TARGET cogutil)
    add_dependencies(atomspace-restful cogutil)
endif()
if(TARGET atomspace-restful AND TARGET atomspace)
    add_dependencies(atomspace-restful atomspace)
endif()
if(TARGET atomspace-agents AND TARGET cogutil)
    add_dependencies(atomspace-agents cogutil)
endif()
if(TARGET atomspace-agents AND TARGET atomspace)
    add_dependencies(atomspace-agents atomspace)
endif()
if(TARGET atomspace-dht AND TARGET cogutil)
    add_dependencies(atomspace-dht cogutil)
endif()
if(TARGET atomspace-dht AND TARGET atomspace)
    add_dependencies(atomspace-dht atomspace)
endif()
if(TARGET atomspace-ipfs AND TARGET cogutil)
    add_dependencies(atomspace-ipfs cogutil)
endif()
if(TARGET atomspace-ipfs AND TARGET atomspace)
    add_dependencies(atomspace-ipfs atomspace)
endif()
if(TARGET atomspace-websockets AND TARGET cogutil)
    add_dependencies(atomspace-websockets cogutil)
endif()
if(TARGET atomspace-websockets AND TARGET atomspace)
    add_dependencies(atomspace-websockets atomspace)
endif()
if(TARGET atomspace-bridge AND TARGET cogutil)
    add_dependencies(atomspace-bridge cogutil)
endif()
if(TARGET atomspace-bridge AND TARGET atomspace)
    add_dependencies(atomspace-bridge atomspace)
endif()
if(TARGET atomspace-metta AND TARGET cogutil)
    add_dependencies(atomspace-metta cogutil)
endif()
if(TARGET atomspace-metta AND TARGET atomspace)
    add_dependencies(atomspace-metta atomspace)
endif()
if(TARGET atomspace-rpc AND TARGET cogutil)
    add_dependencies(atomspace-rpc cogutil)
endif()
if(TARGET atomspace-rpc AND TARGET atomspace)
    add_dependencies(atomspace-rpc atomspace)
endif()
if(TARGET atomspace-cog AND TARGET cogserver)
    add_dependencies(atomspace-cog cogserver)
endif()
if(TARGET atomspace-cog AND TARGET cogutil)
    add_dependencies(atomspace-cog cogutil)
endif()
if(TARGET atomspace-cog AND TARGET atomspace)
    add_dependencies(atomspace-cog atomspace)
endif()
if(TARGET unify AND TARGET cogutil)
    add_dependencies(unify cogutil)
endif()
if(TARGET unify AND TARGET atomspace)
    add_dependencies(unify atomspace)
endif()
if(TARGET ure AND TARGET unify)
    add_dependencies(ure unify)
endif()
if(TARGET ure AND TARGET cogutil)
    add_dependencies(ure cogutil)
endif()
if(TARGET ure AND TARGET cogserver)
    add_dependencies(ure cogserver)
endif()
if(TARGET ure AND TARGET atomspace)
    add_dependencies(ure atomspace)
endif()
if(TARGET pattern-index AND TARGET cogutil)
    add_dependencies(pattern-index cogutil)
endif()
if(TARGET pattern-index AND TARGET atomspace)
    add_dependencies(pattern-index atomspace)
endif()
if(TARGET attention AND TARGET cogserver)
    add_dependencies(attention cogserver)
endif()
if(TARGET attention AND TARGET cogutil)
    add_dependencies(attention cogutil)
endif()
if(TARGET attention AND TARGET atomspace)
    add_dependencies(attention atomspace)
endif()
if(TARGET spacetime AND TARGET cogutil)
    add_dependencies(spacetime cogutil)
endif()
if(TARGET spacetime AND TARGET atomspace)
    add_dependencies(spacetime atomspace)
endif()
if(TARGET dimensional-embedding AND TARGET cogutil)
    add_dependencies(dimensional-embedding cogutil)
endif()
if(TARGET dimensional-embedding AND TARGET atomspace)
    add_dependencies(dimensional-embedding atomspace)
endif()
if(TARGET pln AND TARGET unify)
    add_dependencies(pln unify)
endif()
if(TARGET pln AND TARGET ure)
    add_dependencies(pln ure)
endif()
if(TARGET pln AND TARGET spacetime)
    add_dependencies(pln spacetime)
endif()
if(TARGET pln AND TARGET atomspace)
    add_dependencies(pln atomspace)
endif()
if(TARGET pln AND TARGET cogutil)
    add_dependencies(pln cogutil)
endif()
if(TARGET miner AND TARGET unify)
    add_dependencies(miner unify)
endif()
if(TARGET miner AND TARGET ure)
    add_dependencies(miner ure)
endif()
if(TARGET miner AND TARGET cogutil)
    add_dependencies(miner cogutil)
endif()
if(TARGET miner AND TARGET atomspace)
    add_dependencies(miner atomspace)
endif()
if(TARGET benchmark AND TARGET ure)
    add_dependencies(benchmark ure)
endif()
if(TARGET benchmark AND TARGET cogutil)
    add_dependencies(benchmark cogutil)
endif()
if(TARGET benchmark AND TARGET atomspace)
    add_dependencies(benchmark atomspace)
endif()
if(TARGET moses AND TARGET cogutil)
    add_dependencies(moses cogutil)
endif()
if(TARGET asmoses AND TARGET ure)
    add_dependencies(asmoses ure)
endif()
if(TARGET asmoses AND TARGET cogutil)
    add_dependencies(asmoses cogutil)
endif()
if(TARGET asmoses AND TARGET atomspace)
    add_dependencies(asmoses atomspace)
endif()
if(TARGET learn AND TARGET unify)
    add_dependencies(learn unify)
endif()
if(TARGET learn AND TARGET cogutil)
    add_dependencies(learn cogutil)
endif()
if(TARGET learn AND TARGET cogserver)
    add_dependencies(learn cogserver)
endif()
if(TARGET learn AND TARGET atomspace)
    add_dependencies(learn atomspace)
endif()
if(TARGET language-learning AND TARGET cogutil)
    add_dependencies(language-learning cogutil)
endif()
if(TARGET generate AND TARGET cogutil)
    add_dependencies(generate cogutil)
endif()
if(TARGET generate AND TARGET atomspace)
    add_dependencies(generate atomspace)
endif()
if(TARGET lg-atomese AND TARGET cogutil)
    add_dependencies(lg-atomese cogutil)
endif()
if(TARGET lg-atomese AND TARGET atomspace)
    add_dependencies(lg-atomese atomspace)
endif()
if(TARGET relex AND TARGET cogutil)
    add_dependencies(relex cogutil)
endif()
if(TARGET cheminformatics AND TARGET cogutil)
    add_dependencies(cheminformatics cogutil)
endif()
if(TARGET cheminformatics AND TARGET atomspace)
    add_dependencies(cheminformatics atomspace)
endif()
if(TARGET agi-bio AND TARGET cogutil)
    add_dependencies(agi-bio cogutil)
endif()
if(TARGET agi-bio AND TARGET atomspace)
    add_dependencies(agi-bio atomspace)
endif()
if(TARGET vision AND TARGET cogutil)
    add_dependencies(vision cogutil)
endif()
if(TARGET vision AND TARGET atomspace)
    add_dependencies(vision atomspace)
endif()
if(TARGET sensory AND TARGET cogutil)
    add_dependencies(sensory cogutil)
endif()
if(TARGET sensory AND TARGET atomspace)
    add_dependencies(sensory atomspace)
endif()
if(TARGET visualization AND TARGET cogutil)
    add_dependencies(visualization cogutil)
endif()
if(TARGET visualization AND TARGET atomspace)
    add_dependencies(visualization atomspace)
endif()
if(TARGET TinyCog AND TARGET atomspace)
    add_dependencies(TinyCog atomspace)
endif()
if(TARGET opencog AND TARGET cogserver)
    add_dependencies(opencog cogserver)
endif()
if(TARGET opencog AND TARGET ure)
    add_dependencies(opencog ure)
endif()
if(TARGET opencog AND TARGET attention)
    add_dependencies(opencog attention)
endif()
if(TARGET opencog AND TARGET lg-atomese)
    add_dependencies(opencog lg-atomese)
endif()
if(TARGET opencog AND TARGET atomspace)
    add_dependencies(opencog atomspace)
endif()
if(TARGET opencog AND TARGET pln)
    add_dependencies(opencog pln)
endif()
if(TARGET opencog AND TARGET cogutil)
    add_dependencies(opencog cogutil)
endif()
if(TARGET python-attic AND TARGET cogserver)
    add_dependencies(python-attic cogserver)
endif()
if(TARGET python-attic AND TARGET ure)
    add_dependencies(python-attic ure)
endif()
if(TARGET python-attic AND TARGET pln)
    add_dependencies(python-attic pln)
endif()
if(TARGET python-attic AND TARGET atomspace)
    add_dependencies(python-attic atomspace)
endif()
if(TARGET python-attic AND TARGET cogutil)
    add_dependencies(python-attic cogutil)
endif()
